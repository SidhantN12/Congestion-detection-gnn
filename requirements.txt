# Pinned for reproducibility. Installed via:
#   uv pip install torch==2.9.1 --index-url https://download.pytorch.org/whl/cpu
#   uv pip install torch_geometric==2.8.0.post1 numpy==2.4.6 matplotlib
#   uv pip install streamlit==1.62.0
#
# Python: 3.11.16 (installed via `uv python install 3.11`, since WSL Ubuntu 26.04's
# system Python is 3.14 and has no python3.11 apt package yet)
#
# torch is CPU-only by design (see notes/phase0-environment.md) - install from
# https://download.pytorch.org/whl/cpu, not plain PyPI, or you may get a CUDA build.
torch==2.9.1
torch_geometric==2.8.0.post1
numpy==2.4.6
matplotlib==3.11.1
streamlit==1.62.0
